[install]
registry = "https://registry.npmjs.org"

[test]
coverageReporter = ["text", "lcov"]
coverageSkipTestFiles = true
coveragePathIgnorePatterns = ["**/tests/**", "../ai/**"]
coverageThreshold = { lines = 0.98, functions = 0.99 }
